<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>
      <%= content_for?(:title) ? yield(:title) : 'Bookshop' %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- icons -->
    <script src="https://kit.fontawesome.com/779d459ff2.js" crossorigin="anonymous"></script>

    <%= stylesheet_link_tag "application.scss", "data-turbo-track": "reload", media: 'all' %>
    <%= javascript_include_tag "/../javascript/application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <main>
      <div class="container">
        <%= render 'shared/external_navbar' %>
        <%= render 'shared/flashes' %>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
